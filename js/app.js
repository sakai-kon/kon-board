const config = window.KON_BOARD_CONFIG || {};
const configured = Boolean(config.supabaseUrl && config.supabaseAnonKey);
const app = document.querySelector('#app');
const loginButton = document.querySelector('#login-button');
const adminLink = document.querySelector('#admin-link');
const profileLink = document.querySelector('#profile-link');
const headerProfile = document.querySelector('#header-profile');
let supabase = null;
let currentProfile = null;
if (configured) { const { createClient } = await import('https://esm.sh/@supabase/supabase-js@2'); supabase = createClient(config.supabaseUrl, config.supabaseAnonKey, {auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:true}}); }
function escapeHtml(v=''){return String(v).replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[c]));}
function avatarMarkup(url,name='',size='sm'){return url?`<img class="avatar avatar-${size}" src="${escapeHtml(url)}" alt="${escapeHtml(name)}のアイコン" loading="lazy" referrerpolicy="no-referrer">`:`<span class="avatar avatar-${size} avatar-fallback">${escapeHtml((name||'名').slice(0,1))}</span>`;}
async function currentUser(){if(!supabase)return null;const {data:{user}}=await supabase.auth.getUser();return user;}
async function loadProfile(){currentProfile=null;const user=await currentUser();if(!user)return null;const {data}=await supabase.from('profiles').select('id,display_name,avatar_url,role,account_status').eq('id',user.id).single();currentProfile=data||null;return currentProfile;}
function updateAuthUi(){const signedIn=Boolean(currentProfile);loginButton.textContent=signedIn?'ログアウト':'GitHubでログイン';adminLink?.classList.toggle('hidden',!signedIn||currentProfile.role!=='admin'||currentProfile.account_status!=='active');profileLink?.classList.toggle('hidden',!signedIn);if(headerProfile){headerProfile.innerHTML=signedIn?avatarMarkup(currentProfile.avatar_url,currentProfile.display_name,'md'):'';headerProfile.classList.toggle('hidden',!signedIn);}}
async function renderProfile(){const user=await currentUser();if(!user){location.hash='/login';return;}app.replaceChildren(document.querySelector('#profile-template').content.cloneNode(true));const form=app.querySelector('#profile-form');const input=app.querySelector('#display-name');const message=app.querySelector('#profile-message');input.value=currentProfile?.display_name||'';form.onsubmit=async e=>{e.preventDefault();const display_name=input.value.trim();if(!display_name){message.textContent='表示名を入力してください。';return;}const {error}=await supabase.from('profiles').update({display_name}).eq('id',user.id);if(error){message.textContent=`保存に失敗しました: ${error.message}`;return;}await loadProfile();updateAuthUi();message.textContent='表示名を保存しました。過去の投稿・コメントにもすぐ反映されます。';};}
async function renderHome(){app.replaceChildren(document.querySelector('#home-template').content.cloneNode(true));const list=app.querySelector('#thread-list');const draw=async()=>{const {data}=supabase?await supabase.from('posts').select('id,title,category,content,created_at,profiles(display_name,avatar_url),comments(count)').order('created_at',{ascending:false}):{data:[]};list.innerHTML=(data||[]).map(x=>`<a class="thread" href="#/thread/${x.id}">${avatarMarkup(x.profiles?.avatar_url,x.profiles?.display_name)}<span class="badge">${escapeHtml(x.category)}</span><h3>${escapeHtml(x.title)}</h3><p>${escapeHtml(x.profiles?.display_name||'名無しさん')}</p></a>`).join('')||'<div class="empty">投稿がありません。</div>';};app.querySelector('#search-input').oninput=draw;app.querySelector('#category-filter').onchange=draw;app.querySelector('#new-thread').onclick=()=>location.hash='/new';await draw();}
async function renderLogin(){app.innerHTML='<section class="auth-page"><h1>ログイン</h1><button id="github-login" class="button button-primary">GitHubでログイン</button><a href="#/home" class="button button-secondary">← ホームへ戻る</a></section>';app.querySelector('#github-login').onclick=signIn;}
async function renderNew(){const user=await currentUser();if(!user){location.hash='/login';return;}app.replaceChildren(document.querySelector('#new-template').content.cloneNode(true));app.querySelector('#thread-form').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.currentTarget);const {error}=await supabase.from('posts').insert({author_id:user.id,title:f.get('title'),category:f.get('category'),content:f.get('content')});if(error)return alert(error.message);location.hash='/home';};}
async function renderThread(id){app.replaceChildren(document.querySelector('#thread-template').content.cloneNode(true));const target=app.querySelector('#thread-detail');const {data:post}=await supabase.from('posts').select('*,profiles(display_name,avatar_url)').eq('id',id).single();const {data:comments}=await supabase.from('comments').select('*,profiles(display_name,avatar_url)').eq('post_id',id).order('created_at');if(!post){target.textContent='投稿が見つかりません。';return;}target.innerHTML=`<div class="detail-card">${avatarMarkup(post.profiles?.avatar_url,post.profiles?.display_name,'md')}<strong>${escapeHtml(post.profiles?.display_name||'名無しさん')}</strong><h1>${escapeHtml(post.title)}</h1><p>${escapeHtml(post.content)}</p></div><section class="comments"><h2>コメント</h2>${(comments||[]).map(c=>`<div class="comment">${avatarMarkup(c.profiles?.avatar_url,c.profiles?.display_name)}<strong>${escapeHtml(c.profiles?.display_name||'名無しさん')}</strong><p>${escapeHtml(c.content)}</p></div>`).join('')}<form id="comment-form"><textarea name="content" required></textarea><button class="button button-primary">コメントする</button></form></section>`;target.querySelector('#comment-form').onsubmit=async e=>{e.preventDefault();const user=await currentUser();if(!user){location.hash='/login';return;}const {error}=await supabase.from('comments').insert({post_id:id,author_id:user.id,content:new FormData(e.currentTarget).get('content')});if(error)return alert(error.message);renderThread(id);};}
async function renderAdmin(){app.innerHTML='<section class="admin-page"><h1>管理画面</h1><p>管理機能は既存のままです。</p></section>';}
async function router(){const p=location.hash.slice(1)||'/home';if(p==='/login')return renderLogin();if(p==='/profile')return renderProfile();if(p==='/new')return renderNew();if(p==='/admin')return renderAdmin();if(p.startsWith('/thread/'))return renderThread(p.split('/')[2]);return renderHome();}
async function signIn(){const {error}=await supabase.auth.signInWithOAuth({provider:'github',options:{redirectTo:window.location.origin+window.location.pathname}});if(error)alert(error.message);}
loginButton.onclick=async()=>{const user=await currentUser();if(user){await supabase.auth.signOut();currentProfile=null;updateAuthUi();location.hash='/home';}else location.hash='/login';};
if(supabase){await loadProfile();updateAuthUi();supabase.auth.onAuthStateChange(async()=>{await loadProfile();updateAuthUi();router();});}window.addEventListener('hashchange',router);await router();